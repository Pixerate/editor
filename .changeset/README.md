# Changesets

Hello and welcome! This folder contains Changeset files that track package versioning and changes.

To create a new changeset:
```bash
pnpm changeset
```
Follow the prompt to select changed packages and bump types (`patch`, `minor`, `major`).
